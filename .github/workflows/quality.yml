name: Website Quality Gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: website-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  production-build:
    name: Production build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: lightworld_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d lightworld_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: "postgresql://postgres:postgres@127.0.0.1:5432/lightworld_ci?schema=public"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate production operations scripts
        run: bash -n ops/*.sh

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install locked dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bunx prisma generate

      - name: Apply PostgreSQL migrations
        run: bun run db:deploy

      - name: Type-check application
        run: bunx tsc --noEmit

      - name: Test security and deterministic intelligence
        run: bun test src/lib/admin-auth.test.ts src/lib/assistant-intents.test.ts src/lib/assistant-conversation-regression.test.ts src/lib/assistant-generative-policy.test.ts src/lib/assistant-retrieval.test.ts src/lib/lead-intelligence.test.ts src/lib/proposal-draft.test.ts src/lib/client-auth.test.ts src/lib/mail.test.ts src/lib/newsletter-campaign.test.ts src/lib/admin-governance-policy.test.ts src/lib/admin-permissions.test.ts src/lib/navigation-content.test.ts src/lib/site-content.test.ts src/lib/contact-content.test.ts src/lib/public-widget-consistency.test.ts src/lib/cookie-consent-content.test.ts src/lib/public-appearance-actions.test.ts src/lib/upload-storage.test.ts src/lib/careers-application.test.ts src/lib/public-rate-limit.test.ts src/lib/login-input.test.ts src/lib/next-config-security.test.ts src/lib/admin-guard-await.test.ts
        env:
          ADMIN_SESSION_SECRET: "ci-only-admin-session-secret-at-least-32-bytes"
          CLIENT_SESSION_SECRET: "ci-only-client-session-secret-at-least-32-bytes"

      - name: Build production application
        run: bun run build
        env:
          NEXT_TELEMETRY_DISABLED: "1"
